<div class="page-header">
  <h1>Pictures   <small>写真とコメントを投稿してみよう！</small></h1>
</div>

<% unless logged_in? %>
  <p>写真投稿にはログインが必要です。ユーザー登録、ログインを行ってください。</p>
<% end %>

<div class="table-responsive-md">
  <table class="table table-borderless table-dark">
    <thead>
      <tr>
        <th scope="col">Image</th>
        <th scope="col">Content</th>
        <th scope="col">User</th>
        <th colspan="3" scope="col"></th>
      </tr>
    </thead>

    <tbody>
      <% @pictures.each do |picture| %>
        <tr>
          <td><% if picture.image && picture.image.url %>
                <%= image_tag picture.image.url, width: "200px" %>
              <% end %></td>
          <td><%= picture.content %></td>
          <td><%= picture.user.name %></td>
          <td><%= link_to 'Show', picture, class: 'btn btn-primary'%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</dev>

<br>

<% if logged_in? %>
  <%= link_to 'New Picture', new_picture_path, class: 'btn btn-primary' %>
<% end %>
